#include<bits/atdc++.h>
using namespace std;

void solution(vector<int> v)
{
    int n = v.size();

    vector<int> temp(n);
    for(int i=0;i<n;i++)
    {
        temp[i] = v[i];
    }

    
}
int main()
{
    int n;
    cin>>n;

    vector<int> v(n);

    for(int i=0;i<n;i++)
    {
        cin>>v[i];
    }

    solution(v);
}